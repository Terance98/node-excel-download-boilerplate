# node-excel-download-boilerplate
This repo contains a sample code that I did that generates an excel file and returns it via an api. To test out host the server locally and past the API url to a browser
